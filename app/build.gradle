plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace 'com.tuapp.monedero' // tu package
    compileSdk 34

    defaultConfig {
        applicationId "com.tuapp.monedero"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }
    // ... resto del bloque android
}

dependencies {
    // todas tus librerías aquí
}

// ESTA LÍNEA VA HASTA EL FINAL, SOLITA
apply plugin: 'com.google.gms.google-services'
